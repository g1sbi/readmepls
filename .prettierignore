node_modules/
dist/
.svelte-kit/
pnpm-lock.yaml

# release-please owns this file
CHANGELOG.md

# PocketBase's own JS dialect, not our style
pocketbase/pb_migrations/
pocketbase/pb_hooks/

# tooling scratch, not project source
.superpowers/

# design source of truth; reformatting churns the palette reference
assets/_banner.html

# Captured real-world payloads, not our source. Extraction is whitespace-
# sensitive (HTML text nodes, heading detection), so reformatting these would
# both invalidate them as faithful captures and mask whitespace regressions.
**/fixtures/
